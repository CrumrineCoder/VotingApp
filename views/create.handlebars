<!DOCTYPE html>
<h2 class="page-header">Create Poll</h2> {{#if errors}} {{#each errors}}
<div class="alert alert-danger">{{msg}}</div>
{{/each}} {{/if}}
<html>

<head>
    <title>Voting App</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500" rel="stylesheet" type="text/css">
    <link href="/public/css/main.css" rel="stylesheet" type="text/css">
</head>

<body>
     {{#if user}}
      <p> 
       <input type="button" id="linkingAccountButton" value="I don't want to link my account to the poll" style="color:blue" onclick="toggle(this);">
    {{else}}
      <p> <a href="/users/login">Login</a> if you wish to link this poll with your account.
    {{/if}}
    <form method="post" action="/polls/create" class="poll">
        <input type="text" name="question" placeholder="Write the Question here">
        <input type="text" name="answer" placeholder="Enter reply">
        <input type="text" name="answer2" placeholder="Enter reply">
        <button type="submit" class="submit btn" value="Send order">Submit</button>
    </form>
    <script type="text/javascript" src="/controllers/creatingPolls.js">
    </script>
</body>

</html>