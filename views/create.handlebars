<!DOCTYPE html>
<h2 class="page-header">Create Poll</h2> {{#if errors}} {{#each errors}}
<div class="alert alert-danger">{{msg}}</div>
{{/each}} {{/if}}
<html>

<head>
    <title>Voting App</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500" rel="stylesheet" type="text/css">
    <link href="/public/css/main.css" rel="stylesheet" type="text/css">
</head>
<body>
        {{#if user}}
        {{else}}
            <p> <a href="/users/login">Login</a> if you wish to link this poll with your account and be able to edit the poll after creation. </p>
        {{/if}}
    <form method="post" action="/polls/create" class="poll">
        {{#if user}}
          <input type="checkbox" name="user" value="link">
          <label>Link this poll to my account?</label>
         {{/if}}
         <p> Options </p>
          <input id="Open" type="checkbox" name="Open" value="Open">
          <label>Allow Users to write their own answers.</label>
          <input id="Multiple" type="checkbox" name="Multiple" value="Multiple">
          <label>Allow Users to reply with multiple answers.</label>
          <input id="Captcha" type="checkbox" name="Captcha" value="Captcha">
          <label>Add reCaptcha to Voting Form to prevent spam and vote tampering.</label>
          <input id="Change" type="checkbox" name="Change" value="Change" >
          <label>Allow Users to change votes after voting.</label>
          <input id="SeeResults" type="checkbox" name="SeeResults" value="SeeResults" >
          <label>Allow Users to see the results before voting.</label>
        <input type="text" name="question" placeholder="Write the Question here">
        <input type="text" name="answer" placeholder="Enter reply">
        <input type="text" name="answer2" placeholder="Enter reply">
        <button type="submit" class="submitButton" value="Send order">Submit</button>
       
    </form>
    <script type="text/javascript" src="/controllers/creatingPolls.js">
    </script>
</body>

</html>